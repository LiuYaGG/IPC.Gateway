scrape_configs:
  - job_name: ipc-gateway
    scheme: https
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/secrets/ipc-gateway-api-token
    static_configs:
      - targets:
          - ipc-gateway.example.com
